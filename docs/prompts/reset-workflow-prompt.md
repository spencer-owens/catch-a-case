Great. Now that we've finished our feature, let's clear the workflow using the following command:
```bash
cp docs/workflow-templates/XXX-workflow-template.md docs/workflow-checklists/XXX-workflow-checklist.md
```
